# Backend API origin used by the React app.
# Vite exposes VITE_* values in the browser, so do not put secrets here.
VITE_API_BASE_URL=https://api.vuapp.dev

# Public file URL origin. Leave it equal to the backend origin unless files move elsewhere.
VITE_PUBLIC_API_ORIGIN=https://api.vuapp.dev
